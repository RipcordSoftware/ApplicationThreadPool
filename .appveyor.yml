platform: Any CPU
configuration: Debug
image: Visual Studio 2017

before_build:
    - cd tests\ThreadPool.Tests.dotnet
    - nuget restore

build:
    project: tests\ThreadPool.Tests.dotnet\ThreadPool.Tests.sln
